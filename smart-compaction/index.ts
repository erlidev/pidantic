import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Entry point reserved for the smart-compaction extension. */
export default function smartCompaction(_pi: ExtensionAPI): void {}
