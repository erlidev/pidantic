# pi supplies @earendil-works/* and typebox to the extension at load time. Installing our own copies
# would both waste ~140MB and risk a second typebox instance being loaded alongside pi's.
legacy-peer-deps=true
